# Core
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.7.0
pydantic-settings==2.3.0
python-dotenv==1.0.1
httpx==0.27.0

# LangGraph / LangChain
# langgraph 0.2.0 required langchain-core<0.3, but langchain-openai 0.2.x requires >=0.3
# langgraph 0.2.28 was the release that lifted the <0.3 cap — use loose pins here
langgraph>=0.2.28,<0.3
langchain-core>=0.3.0,<0.4
langchain-openai>=0.2.0,<0.3

# Database
aiosqlite==0.20.0

# Embeddings + Search
sentence-transformers==3.0.0
faiss-cpu==1.8.0
torch==2.3.0
numpy==1.26.4

# PDF parsing
pdfplumber==0.11.0
pymupdf==1.24.0

# Data
requests==2.32.0
tqdm==4.66.0
pandas==2.2.0

# Fine-tuning (only needed on GPU droplet)
# llamafactory  # install separately on GPU droplet
